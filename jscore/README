JavaScriptCore, OSB Javascript engine
-------------------------------------
JavaScriptCore is javascript interpreter that OSB uses. It is based on
Apple's WebCore JavaScriptCore code, which is in turn based on KDE kjs
code.

Directories:
kjs/		# the javascript interpreter itself
pcre/		# perl compatible regular expression library (PCRE)




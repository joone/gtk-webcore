#ifndef kwq_algorithm_included
#define kwq_algorithm_included

namespace std {

inline int min(int a, int b)
{ 
	return (a<b) ? a : b; 
}

inline int max(int a, int b)
{ 
	return (a<b) ? b : a; 
}

template<class T>
inline void swap(T &a, T &b) {
    T tmp = b;
    b = a;
    a = tmp;
}

}

#endif
